# Roadmap

Anthropic spends billions of dollars and years with thousands of GPUs on foundation models. That's not this. Transformers themselves are only from 2017 (the "Attention Is All You Need" paper), the whole field is young enough that a lot of useful ground is still coverable by one person on a Mac Mini, as long as the goal is calibrated to the hardware.

### Phase 0: Pipeline proof (now, days)
LoRA fine-tune of a small open base (Qwen2.5-0.5B-Instruct) on our own Obsidian wiki + project READMEs. 588 lines of data, 200 iterations, runs in minutes on the M4. Goal: prove the loop works end to end (data → train → adapter → serve), not quality. Done, no gibberish (unlike Arthur).

### Phase 1: More data, same model (weeks 1-3)
Feed it everything: journal entries, commit messages, project READMEs/WHITEPAPERs, roadmap.md files, notes vault, even old Slack/iMessage exports if we want the voice right. Thousands of chunks instead of hundreds. Re-run LoRA. This is the single highest-leverage step, small models improve far more from 10x the data than from 10x the iterations.

### Phase 2: Evaluate like it matters (weeks 2-4, parallel with Phase 1), started
`eval/prompts.jsonl` has 28 real prompts, `eval/run_eval.py` generates against any adapter. See `eval/` for run-by-run results. A model that "trains" but never gets graded is a number going down, not progress.

### Phase 3: Bigger base, same recipe (month 2)
Once the pipeline is boring and repeatable, try a bigger base. Candidates as of Sept 2026: `Qwen3.5-0.8B` (direct successor to what we're using now), `SmolLM2-1.7B` (fully open training recipe, worth it if transparency matters to us), `Llama-3.2-1B-Instruct` (solid middle ground, ~1GB at Q4). Bigger model = slower training, more memory, better baseline fluency. Compare quality per minute of training against the 0.5B, there's a real chance the 0.5B fine-tuned on great data beats a bigger base fine-tuned on so-so data.

Started early, blocked on this hardware: `Qwen3.5-0.8B-4bit` caused three crashes in a row (two system-memory, one Metal/GPU OOM mid-backprop even with plenty of free RAM). That last one is the real signal, it's a genuine fit problem, not a "too many things running" problem. `ada-1b-adapter/` sits unfinished.

Retried twice more after building `run_lora_capped.py` (caps MLX's Metal cache/memory, fixed the *growth-over-iterations* OOM pattern for the 0.5B model). Both retries crashed the machine to near-zero free memory within about 15 seconds of starting, once right after a clean restart with 6.4GB free. That's fast enough and repeatable enough to rule out "other sessions competing for memory" as the explanation, this specific model's baseline footprint (not its growth pattern) doesn't fit this machine's training memory budget, even capped. Not the same bug the cache-limit fix solved. Genuinely done trying this model on this hardware without either a smaller batch/rank config or different hardware, not a blind retry a fourth time.

### Phase 4: Retrieval instead of memorization (month 2-3)
Don't try to cram every project fact into model weights, that's what causes hallucination and stale knowledge. Wire it to `brain` (the existing RAG-over-notes setup) so the model reasons over live retrieved context instead of "remembering" it. Small fine-tuned model + good retrieval beats a bigger model with neither. This is the actual production architecture, not a toy.

### Phase 5: Give it a job (month 3+), started
Once retrieval works, point it at concrete, boring, checkable tasks:
- **In-voice drafting**, journal entries, commit messages, README sections in house style (no em dash, no AI voice, sans-serif brain already enforced elsewhere, teach the model the same rules)
- **Project Q&A**, "what's the status of Epiphany", answered from the wiki instead of us re-reading MEMORY.md
- **Local autocomplete**, a tiny always-available model that doesn't hit the network, for quick text expansion
- **A judge/filter model**, small models are cheap enough to run on every commit or PR as a first-pass linter before anything hits a bigger model

Concrete backlog, honest scope (this makes it *feel* like a real assistant to use, it does not and cannot make a 0.5B model "as good as Claude/GPT", see Phase 6/"never do" below):

More backlog, same honest scope, feel-like-a-real-assistant not compete-with-Claude:

Measured real latency/memory instead of guessing: an FAQ match is ~0.04s/26MB (no model load), a generation-path answer is ~2s/570MB. Documented in FAQ.md and added to the main eval set, now 29/29 + 11/11 = 40/40. Also found while testing this: one retrieved source in brain's index is `~/debug-test.md`, a stray vector left over from an earlier debugging session with no real file backing it. brain has no delete-by-source API yet, not worth building one for a single stray vector tonight, noted here so it doesn't get mistaken for a live doc later.

Real QA gap closed: `eval/score.py` only ever exercised `ask.py`'s `ask()`, chat.py's own logic (history-window building, echoed-scaffold stripping) had zero automated coverage, only a one-time manual check when the indentation bug got fixed. Added `test_chat.py`, 6 pure-function tests, no model calls, no interactive input needed. Wired into the pre-commit hook alongside score.py, now blocks a chat.py regression too.

Fixed the actual "who's the president" case, the original motivating example for this whole feature. `general_knowledge()`'s DDG/Wikipedia fallback only ever described the office, never today's holder, that was documented as an accepted ceiling. Not actually a hard ceiling: added `current_officeholder()`, a Wikidata lookup for "who is/who's the X" questions. First attempt used "no P582 end-date qualifier" as the current-holder signal and it was wrong, Wikidata pre-fills the *expected* end of a fixed term even on the sitting officeholder, so the US president lookup kept returning nothing. Real fix: use `rank: preferred`, that is literally Wikidata's own flag for which value among several historical ones is current. Confirmed correct for "who is the president of the united states" (sitting president) and "who's the prime minister of canada" (Mark Carney). Added to the held-out set since it's a genuinely new capability, not a memorized fact, 29/29 + 12/12 = 41/41 now.

Found the fix above only worked through `ask.py`, `chat.py` (the thing people would actually run to have a conversation) never called `general_knowledge()` at all, only FAQ-match and retrieval. Wired it in, gated the same way (`is_project_question`, skip if FAQ already matched, no wasted network call). Manually verified: chat.py now answers "who is the president" correctly and still resolves project questions via FAQ. Not added to test_chat.py, that file is deliberately pure-function/no-network so it stays fast on every commit, general_knowledge is a live API call and belongs in a manual/eval check instead.

Stress-testing that officeholder fix immediately caught a real regression: "who is the current president" false-matched `faq_match()`'s "What's the current eval score?" entry (0.56, just over the 0.55 threshold), the word "current" alone was enough shared-keyword overlap to win. Root cause, not the symptom: "current" is a generic temporal filler word, same category as "now"/"right" which were already in `_STOPWORDS`, added it there. Also reordered `ask()`/`chat()`: a live officeholder lookup for a "who is/who's the X" question now runs before `faq_match()`, not after, so a static FAQ file never gets a chance to answer a question whose real answer changes over time. Confirmed no other regression across 29/29 + 12/12 + 6/6.

- **2026-09-13, 28/28 (100%).** Fixed the 5 remaining `eval/score.py` failures. Three were genuine content gaps (LoRA-vs-full-fine-tuning, "used for eventually"/"never do on hardware", a genuinely short one-sentence-summary entry all got their own dedicated FAQ entries, the real facts already existed elsewhere but weren't titled the way people actually ask). The "hallucinate" question exposed a real scoring bug: `faq_match()`'s pure sentence-shape similarity (difflib) picked a lexically-similar-but-wrong entry over the topically-correct one even after the keyword-overlap *gate* was added, because the gate was binary (has overlap or not) not weighted. Fixed by blending keyword-overlap fraction directly into the match score (50/50 with the difflib ratio), not just gating on it. Confirmed no regression on the two earlier precision fixes (capital-of-France false-positive, who-maintains).

Real QA infrastructure, 2026-09-13: every prior eval round meant reading 28 free-text answers by hand and judging correctness by feel. Built `eval/score.py` instead: each `prompts.jsonl` entry now carries objective pass criteria (`must_contain`, `must_contain_any`, `must_not_contain`, style limits for generative prompts). Run `./.venv/bin/python eval/score.py` for a real number, `--verbose` for the per-prompt breakdown, exits nonzero on any failure so it could gate a commit hook later.

First automated run: **23/28 (82%)**. Caught a real bug the eyeball method missed: `faq_match()`'s lexical similarity (`difflib`) sometimes matches the *wrong* FAQ entry when two questions share surface wording but differ in meaning ("what should Samantha be used for eventually" matched to the "beat Claude/GPT" entry instead of the Phase 5 entry). Tried a token-overlap (Jaccard) alternative, didn't cleanly fix it either, both are lexical, not semantic. Real fix needs actual semantic similarity (embeddings, e.g. reusing `brain`'s search infra scoped to just `FAQ.md`'s pairs), that's real follow-up work, not a threshold tweak.

- **2026-09-13, run 7: real retrain on doubled data.** Six prior runs concluded a 0.5B LoRA on ~150-300 examples learns style, not facts, retrieval was built as the fix instead of chasing more training. Since then FAQ.md and eval writeups roughly doubled the real, distinct own-project content (28 to 55 own-doc chunks, once `prep_data.py`'s hang was fixed, see below). Worth a real retrain to check if that structural conclusion still holds. Ran 200 iters via `train_resilient.sh`, resumed from the existing adapter, attempt 1 finished cleanly, no crash, no restart needed, peak mem 4.28GB. Val loss dropped 2.13 to 1.98.

  Eval result: **28/29 (was 29/29 pre-retrain), 12/12 holdout unchanged.** The one miss: "write a one-line commit message" now rambles into a fake markdown doc section ("## Fixed in 2026-09-30 training run...") instead of a clean one-liner, it's picking up FAQ.md's own doc-section voice for a task that should sound nothing like a FAQ entry. Real, honest regression, not a rubric-gaming issue, confirmed by reading the actual output. Everything FAQ-matched, retrieved, or officeholder-looked-up is unaffected, this only touches the pure-generation path, consistent with six earlier runs: more data still doesn't reliably fix generative-task quality at this scale, and doubling it here made one narrow case worse, not better. Kept the new adapter anyway (lower val loss, real new facts baked in, one generative miss out of 29 is a fair trade), documented honestly rather than reverting to chase a clean number.

- **2026-09-13, real bug: `prep_data.py` could hang indefinitely.** The Obsidian vault is a live iCloud folder, an evicted (not locally cached) file's read could block on iCloud's file provider for 100+ seconds. Worse than expected to fix: an in-process `signal.alarm()` timeout did not reliably interrupt it (watched one file stay blocked 140s+ past a 5s alarm), and even killing the reading subprocess outright didn't obviously help at first glance, the block looked kernel-level. Turned out to be a false alarm on the timeout mechanism itself, not a truly unkillable process: the parent's cumulative elapsed time across all ~343 files was being misread as time-stuck-on-one-file, leading to killing a healthy, still-progressing run twice before letting a subprocess-timeout version actually finish. It completed normally. Real fix that shipped: shell out to `cat` per file with a hard `subprocess.run(timeout=5)`, a genuine improvement over the in-process signal either way since it can't be blocked by anything the reading process itself gets stuck on.

- **2026-09-13, run 8: real instruction examples fix the run 7 regression.** Run 7's one miss ("write a one-line commit message" rambling into fake FAQ-doc markdown) had a real, identifiable cause: `prep_data.py`'s only template is "Tell me about {topic}." -> doc content, the model had never seen a single example of a genuine task instruction paired with a terse, plain response. Fix: `TRAINING_EXAMPLES.md`, 12 real instruction/response pairs, not invented, actual commit messages harvested straight from this repo's own `git log` (the exact house voice this project already writes in) paired with the instruction that would have produced them. Added a small `read_examples()` parser to `prep_data.py` (same `## header` style as FAQ.md) since the generic template can't represent an instruction, only a topic. Retrained 200 iters again, resumed. Fixed: 29/29 + 13/13, the commit-message prompt now returns a clean real line ("Fix a real MLX memory leak during long unattended runs"), confirmed not verbatim-parroting a training example by testing an unseen phrasing ("add dark mode support") and getting a different, still-clean line.

  That same unseen-phrasing test caught a second, separate real bug: `general_knowledge()` was firing on task instructions, not just questions, "write a commit message for adding dark mode support" got hijacked into Wikipedia's Git article (DDG's instant-answer API apparently has a loose match for "commit" in there somewhere). Fixed with `is_question()`, gates `general_knowledge()` to only fire on something actually shaped like a question (ends in "?" or starts with a question word), never an imperative instruction. Added a held-out prompt for it. 29/29 + 13/13 + 6/6 = 48/48 across all three suites after this round.

  Also worth noting honestly: this retrain's resilient wrapper needed 6 attempts before starting, not because of a crash, its own pre-flight memory check (`MIN_FREE_MB=500`) correctly waited out a real low-memory moment on the machine (148MB free at one point) with backoff instead of starting into an OOM. Working as designed, not a new bug.

Removed `eval/run_eval_rag.py`: predates `eval/score.py`, prints prompt/expect/got with no scoring, genuinely superseded by `eval/score.py --verbose` (same output plus real pass/fail). Confirmed zero references anywhere before deleting, dead code, not a stale-but-linked tool.

Found a real latent bug auditing `FIXED_FACTS`: the who-maintains pattern (`who.*(maintain|own|wrote|author)`) matches any "who wrote X" question, not just ones about this project. `general_knowledge()` currently intercepts most of those first, masking it, but that's not guaranteed, simulated it failing and confirmed "who wrote Romeo and Juliet" confidently answered "Joshua Trommel". Fixed by requiring actual project context (project/repo/repository/turing/samantha) alongside the verb, not just the verb alone. Confirmed the real intended case ("Who maintains this project?") still works and the unrelated case now correctly falls through instead of confidently lying.

- **2026-09-14, same audit style found a second, live false-positive: the "blocked" `FIXED_FACTS` entry.** `\bblocked\b.*\b(project|right now)` isn't project-specific either, "project" and "right now" are generic. Confirmed directly (no simulation needed, `try_extract` doesn't gate on retrieval results): "Is the Conveyer project blocked right now?" and even "Why is my sink blocked right now?" both confidently returned Turing's Qwen3.5-0.8B canned answer. Also found the old pattern was word-order-dependent (`.*` requires "project" to appear *after* "blocked"), so "Is this project blocked?", the actual real phrasing, never matched at all. Fixed with the same lookahead approach as the who-wrote fix: require `this project`/`turing`/`samantha` explicitly, order-independent. Verified 2 true positives now match and stayed matching, 3 false positives (including the exact pre-fix repro) now correctly fall through. 29/29 + 13/13 + 6/6 after the fix.

- **2026-09-14, finished the audit: the same order-dependency bug was in every remaining `.*`-based pattern in the file.** Checked the third `FIXED_FACTS` entry (loss chart) and all three `EXTRACTORS`. The loss-chart pattern (`\bloss chart\b.*\bdata\b`) missed the natural phrasing "What data feeds the loss chart?" for the identical reason as the blocked-project fix, "data" appeared before "loss chart" in the string. Two `EXTRACTORS` had the same shape: `what tool.*train` missed "The training uses what tool?", `LoRA.*stand` missed "What does the acronym stand for, LoRA?". These aren't confident-wrong-answer bugs like the first two, they're silent misses that fall through to generation, but this file's own top comment says generation "invents plausible-but-wrong specifics" for exactly these facts, so a missed extraction still degrades to a worse answer, not a neutral one. Fixed all three with the same order-independent lookahead form. Confirmed each previously-failing phrasing now matches, confirmed the already-passing phrasings didn't regress. 29/29 + 13/13 + 6/6 after the fix. No `.*`-based question patterns left in `EXTRACTORS`/`FIXED_FACTS` to audit.

### Phase 8: Large-scale training data (2026-09-14 evening)

Harvested 183 real instruction/response pairs from fleet git history and rebuilt training data from 55 chunks to 985 total chunks. Deferred retrain earlier due to low memory (0.4GB free, machine crashed mid-backprop from 6.4GB-free twice before). Launched retrain at 01:22:04 via `train_resilient.sh "mlx-community/Qwen2.5-0.5B-Instruct-4bit" "ada-1-adapter" 200` (resumed from checkpoint), finished cleanly at 01:27:56 with no crashes. Training time: 5m52s.

**Eval after retrain (all suites measured against pre-training baseline):** 29/29 (no regression on core eval), 17/18 on held-out (one honest regression: "Is there a menu bar app for this project?" changed from direct answer to "Yes, there is..." preamble, style rubric hit), 18/18 on test_chat.py (no regression), 13/16 on faq_paraphrase (no regression), Nimble integration PASS. Net change: 29/29 + 17/18 + 18/18 + 13/16 = 77/81 (95%, baseline was 78/81 = 96%). One honest miss on a stylistic rubric constraint, but no factual regression, answer is correct in content. Trade-off: held new architecture with ~930 new training examples, lost one style consistency on trivial phrasing. Kept new weights (lower val loss, real new facts, acceptable trade).

- **2026-09-14, moved on to `chat.py`, found a real leak, not a miss.** `clean()`'s scaffold-stripping loop guarded on `idx > 0`, so a marker found at index 0 (the model's output starting *immediately* with `User:` or `Samantha:`, no leading newline) was never stripped. Confirmed: `clean("User: what else?\nSamantha: x", "q")` returned the untouched echo, the raw prompt scaffold, verbatim, as if it were Samantha's real answer. This is worse than the ask.py misses, those degrade to a worse generated answer, this one shows the user internal plumbing. Fixed by changing the guard to `idx >= 0`; an empty string on the position-0 case is an honest "nothing usable" instead of a scaffold leak. Added `test_clean_strips_echo_at_position_zero` (was completely uncovered before, both existing `clean()` tests only used markers with real text before them). 29/29 + 13/13 + 7/7 after the fix.

- **2026-09-14, v0.7.0: stopped auditing regex in isolation, actually ran the CLI with real questions.** Fired a batch of live `ask.py` queries covering every code path and found three more real, confirmed bugs the eval suite hadn't caught, none of them regex order bugs this time:
  1. **`faq_match()` confidently returned the wrong FAQ entry.** "Is this project blocked?" scored higher against "Who maintains this project?" (shares only "project") than the correct "What's blocked or paused right now?" (shares only "blocked"), pure sentence-shape coincidence. First fix attempt (blanket IDF-weight the whole overlap score) traded this bug for a new one: it then lost "What's the difference between Turing and Samantha?" to the wrong LoRA/fine-tuning FAQ entry, because on a 37-entry corpus a word can be rare (low document frequency) without being topically meaningful ("difference" appears in only 1 FAQ question purely by phrasing accident). Real fix: keep flat keyword-overlap as the primary signal (already correct whenever shared-word *count* differs), use word rarity only to break ties when two entries share the exact same number of words, the one case flat counting genuinely can't resolve.
  2. **`is_project_question()` was missing the word "project" itself.** "Is this project blocked?" contains none of the named keywords (turing/samantha/etc.), so it wasn't recognized as project-scoped, fell through to `general_knowledge()`, and Wikipedia confidently returned "List of websites blocked in mainland China", a completely unrelated wrong answer. There's no other project this CLI could mean by "this project". Added "project"/"repo"/"repository" to `PROJECT_KEYWORDS`.
  3. **`try_extract()` let generation hallucinate on exactly the facts it exists to protect.** "What tool trains the adapter?" matched an `EXTRACTORS` question shape (a precision-sensitive fact), but the real answer chunk (FAQ.md's own "## What tool actually runs training?") didn't rank in the top retrieved results for this phrasing, a genuine `brain` retrieval-ranking gap, not a Turing regex bug, not chasing it here. Falling through to generation produced a real fabrication ("Adversarial Training for Speech, ATR"). Since this file's own top comment already documents generation as unreliable for exactly this fact class, added an honest "I don't have that pinned down, not going to guess" fallback instead of letting it invent specifics once we already know we're in that category.

  Added 3 new holdout prompts locking in all three fixes (16/16). 29/29 + 16/16 + 7/7 after the fix. Minor version bump, this changed actual answer behavior across several question shapes, not just regex robustness.

- **2026-09-14, v0.7.1: ran actual multi-turn conversations, found the memory feature didn't work for the obvious case.** Asked "What is Turing?" then "What is its first model called?" in one `chat.py` session. The follow-up has no project keyword of its own ("its" refers back, nothing else), so `is_project_question()` returned False, it fell through to `general_knowledge()`, and got answered with a generic "what is an LLM" Wikipedia-style definition, no memory of the prior turn at all, the one thing `chat.py` exists to add over `ask.py`. Fixed with `_is_project_followup()`: if the previous turn was project-scoped and the current one has a back-reference pronoun (it/its/this/that/these/those), treat it as a continuation instead of routing it to the web. Retested the same conversation, "its first model" now correctly answers "Samantha". Added 4 tests for the new helper (11/11 in `test_chat.py`). 29/29 + 16/16 + 11/11 after the fix.

- **2026-09-14, v0.7.2: same "PROJECT_KEYWORDS missing a word" bug, fourth time.** Ran another real multi-turn chat, "Is the model good?" (no pronoun, no named entity, just the bare word "model") matched nothing in `PROJECT_KEYWORDS`, fell through to `general_knowledge()`, and Wikipedia confidently returned the "Bill Emerson Good Samaritan Food Donation Act", a completely unrelated law. Same root cause and same fix as "project"/"repo"/"repository" in v0.7.0: there's no other model this CLI could mean by "the model", so the bare word is itself a reliable signal. Added "model" to `PROJECT_KEYWORDS`. Same session also surfaced a real but lower-severity issue, "What causes it to hallucinate?" produced a garbled, semi-incoherent generated answer, not confidently wrong, just low quality. Traced it to `faq_match()`'s phrasing sensitivity (the real FAQ entry uses "why does...hallucinate", shares only one content word with the query, not enough overlap to clear the threshold). Deliberately not reworking `faq_match()`'s scoring a third time this session, that algorithm already broke once from a well-intentioned fix earlier tonight, and "garbled but not deceptive" is a lower-severity, already-documented limitation ("quality varies, that's the honest ceiling") than a confident wrong answer in an unrelated domain. Added a holdout prompt for the model-keyword fix (17/17). 29/29 + 17/17 + 11/11 after the fix.

- **2026-09-14, v0.7.3: the pronoun-followup fix from v0.7.1 was too narrow, replaced it with a sticky topic flag.** Real chat: "What is the FAQ matcher?" then "How confident does a match need to be?" The follow-up has no project keyword AND no pronoun (nothing for `_is_project_followup`'s regex to catch), fell through to `general_knowledge()`, Wikipedia confidently returned an article about a Chinese comedian's unrelated "confident" catchphrase. No amount of pattern-matching the current question's own wording generalizes to every way a person continues a topic without repeating themselves. Replaced the whole approach: `chat.py`'s startup banner already says "Samantha, Turing project assistant", so once any turn in a session is genuinely project-scoped, a plain sticky `topic_active` flag threaded through the loop keeps every later turn in scope too, no per-question reference-detection needed, simpler than what it replaced. One carve-out preserved: a "who is/who's the current X" question always overrides the sticky flag, that's a real tested live-lookup escape hatch (see v0.6.0's officeholder fix), not something a resumed project topic should swallow. Verified: the new failure case, the original v0.7.1 case, and a genuine standalone general-knowledge question (capital of France) all now route correctly. Replaced 4 old tests with 4 new ones for the pure `project_scope()` function. 29/29 + 17/17 + 11/11 after the fix.

- **2026-09-14, v0.7.4: testing the officeholder carve-out itself found the word "current" silently breaking it.** Live chat: "What is Turing?" then "Who is the current prime minister of Canada?" The sticky-flag carve-out correctly let the who-question through, but `current_officeholder()` itself returned nothing, Wikidata's entity search was literally asked to find a page named "current prime minister of Canada" (the real entity is just "Prime Minister of Canada"), got zero hits, and silently failed. That fell through to `faq_match()`, which then confidently matched the wrong thing: `FAQ.md`'s own "Can it answer 'who's the president'..." entry, a description *of* the feature, not an actual answer, so the chat printed documentation about itself instead of "Mark Carney." Fixed by stripping "current"/"present"/"sitting" (plus "the", repeated) from the office name before searching, they add no entity identity, every officeholder answer is already "as of now" by construction. Also hit a real 429 from Wikidata mid-debugging, purely from this session's own rapid-fire testing tonight, not a code bug, confirmed clean once it cleared. Logging, not fixing this cycle: a genuine transient Wikidata failure still falls through to that same misleading self-referential FAQ answer instead of an honest "couldn't look that up," a real resilience gap, lower priority than the always-reproducible bug this cycle actually fixed. Added a holdout regression prompt (18/18). 29/29 + 18/18 + 11/11 after the fix.

- **2026-09-14, real CLI/TUI bug: shipped a feature with zero docs, immediately hijacked itself.** Added `./samantha` (plain CLI launcher) and `./samantha --tui` (curses full-screen mode, stdlib only) so the project boots straight into a conversation instead of needing `source .venv/bin/activate && python3 chat.py` spelled out. Live-tested it the same way as every other cycle this project: asked real questions about the thing just built. "How do I boot into you?" got hijacked into a generic Wikipedia article on computer booting, "what's the TUI mode called" got hijacked into a Call of Duty: Modern Warfare plot summary, neither is remotely related. Root cause wasn't a regex bug this time, it was a documentation gap: nothing in FAQ.md or brain's index mentioned the new launcher at all, so there was nothing for retrieval or FAQ-matching to find, and both questions fell through to `general_knowledge()`'s web fallback with no project-specific content to compete against. Fixed by adding two real FAQ.md entries (how to boot into Samantha, whether there's a TUI). Retested both exact questions, both now answer correctly and specifically instead of hallucinating unrelated web content. 29/29 + 18/18 + 11/11 = 58/58, no regressions.

- **2026-09-14, real topic_active bug: a follow-up after an FAQ-matched answer wasn't sticky.** "How do I boot into you?" FAQ-matched correctly (see above), but its own wording has no `PROJECT_KEYWORDS` hit, so `topic_active` stayed False. The next turn, "what if I don't pass any flags", also had no keyword and no pronoun, fell through to `general_knowledge()`, and Wikipedia returned an ISIS flag-history article, worse than the earlier CoD/booting misses, actively alarming for a completely unrelated CLI-flags question. Root cause: `topic_active` was only ever set by re-deriving scope from each question's own wording (`is_project_question()`), never from how the previous turn's answer actually got produced. Fixed: latch `topic_active` True whenever the answer came from an FAQ match, extraction, or project-context generation, not just keyword detection. Retested the same two-turn conversation: no more hijack, stays on-topic. Follow-up itself still isn't factually great (generation-path hallucination on a genuinely obscure phrasing, not FAQ-matched even after adding a dedicated new entry for it), same class of already-documented ceiling as v0.7.2 (faq_match phrasing sensitivity), not reworking the matcher a third time for it. 29/29 + 18/18 + 11/11 after the fix, no regressions.

- **2026-09-14, real UX bug: "bye" didn't quit.** `chat()`/`tui()` only recognized `"exit"`/`"quit"` as stop words. Live-tested `"bye"`, a completely natural way to end a conversation, and it fell through to generation instead: a garbled answer including a stray "NOT FOR SALE" line, confusing output for what should have been a silent, instant exit. Fixed with `EXIT_WORDS = ("exit", "quit", "bye", "q")`, both loops now check against it. Retested: "bye" exits cleanly with no generated text. 29/29 + 18/18 + 11/11 after the fix, no regressions. (Also live-tested "what has Turing looked at for inspiration" and "does Samantha remember me between sessions" in the same pass: the first produced garbled-but-not-deceptive generation, same already-documented quality ceiling as prior sessions, not chased; the second correctly and tersely answered "no", genuinely correct, not a bug.)

- **2026-09-14, real leak: FAQ-doc voice bled a spurious markdown header into a real answer.** Live-tested a meta question about the project's own code ("how does the sticky topic flag interact with the officeholder carve-out"). Generation rambled past a coherent (if imperfect) answer straight into a fabricated `## What is the honest win condition for the user?` section header and kept generating unrelated content after it, the model picked up FAQ.md's own `## Question` structure from training data and echoed it mid-answer, same leak class as the earlier `User:`/`Samantha:` scaffold bug (v0.6.6), just a different scaffold. `clean()` only ever stripped the chat-turn markers, never markdown headers. Fixed with the same pattern: cut at the first `\n##`, a real answer never legitimately contains one. Retested the exact repro, no more header leak. 29/29 + 18/18 + 11/11 after the fix, no regressions.

- **2026-09-14, real FAQ mismatch: "what's the difference between ask.py and chat.py" confidently returned the LoRA-vs-fine-tuning entry instead.** No FAQ entry directly answered that exact framing (only a "what is chat.py?" entry existed, no head-to-head comparison), so `faq_match()`'s difflib scoring latched onto surface wording ("difference between") shared with the LoRA entry instead. Added a dedicated `## What's the actual difference between ask.py and chat.py?` entry. Retested the exact question, now matches correctly. 29/29 + 18/18 + 11/11 after the fix, no regressions.

- **2026-09-14, stopped patching FAQ entries and measured the matcher instead: real recall is 3/16.** Three consecutive fixes this session were all "add another FAQ entry," which is the exact whack-a-mole `faq_match()` was built to replace. Built `eval/faq_paraphrase.py` to get a number: 16 natural rephrasings of questions FAQ.md genuinely answers ("who works on this", "why did arthur fail", "does training cost money"), each paired with the header that should answer it. **3/16 matched the right entry, 12 matched nothing, 1 matched the wrong one.** The 29/29 main eval score is measuring FAQ-worded questions against the FAQ, so it never saw this: real phrasing falls off a cliff.

  Swept `FAQ_MATCH_THRESHOLD` from 0.55 down to 0.20 to check whether it was just tuned too strict. It isn't, and the sweep is the useful part: recall only climbs 3/16 to 7/16 while wrong-entry matches climb 1 to 5, and out-of-scope false positives get worse too. Threshold tuning cannot fix this, which is direct evidence for the conclusion the 2026-09-13 entry already predicted on theory alone: lexical similarity is the wall, and real semantic matching (embeddings, reusing brain's own search infra scoped to FAQ.md's pairs) is the actual fix. Now backed by numbers instead of a hunch.

  The sweep also exposed **two live confident-wrong answers at the current threshold**, the project's highest-severity bug class: "is my sink blocked right now" returned Turing's Qwen3.5-0.8B pause status (matched "What's blocked or paused right now?" on the single word "blocked"), and "what is the difference between python and javascript" returned the ask.py-vs-chat.py answer (matched on "difference" alone). The second one is an honest self-inflicted regression: that FAQ entry was added *earlier this same session* to fix a different miss, and it immediately created a new false positive, which is precisely the cost of whack-a-mole that the measurement above quantifies. The first is a sibling-path miss: the 2026-09-14 "blocked" fix patched `FIXED_FACTS` only and left `faq_match()` with the identical bug, never grepped for the other caller.

  Fixed both without touching the threshold. A question carrying more words the FAQ has never seen (document frequency 0) than words it shares with an entry is a question about something else, riding on generic filler overlap. First attempt vetoed whenever the question's *rarest* word was unknown, which killed recall outright (3/16 to 0/16) because ordinary verbs like "come" and "outside" are unknown too without being the subject. Counting unknowns against shared words instead separates the cases cleanly. Result: false positives 2/8 to 0/8 on the out-of-scope set, recall unchanged at 3/16, no cost. 29/29 + 18/18 + 11/11 after the fix.

  Open, with evidence now attached: recall is still 3/16. That is the real headline and it is not fixable lexically. Next: semantic FAQ matching via embeddings.

- **2026-09-14, recall 3/16 to 5/16 on stemming and a three-letter-acronym bug, and the semantic wall confirmed from a second direction.** Diagnosed all 12 paraphrase misses individually instead of assuming they needed embeddings. Most weren't semantic at all: four shared zero keywords purely from morphology (train/training, scoring/score, evals/eval, matching/matcher), the matcher simply couldn't see through an `-ing`. Added `_stem()`, a dozen lines of suffix stripping, deliberately not a real Porter stemmer, both sides stemmed identically so over-stripping stays symmetric. Separately, `_keywords()` filtered to `len(w) > 3`, which silently discarded this project's most distinctive words: `faq`, `tui`, `cli`, `api` are all three letters. Lowered to `> 2`. Together: recall 3/16 to 5/16, wrong-entry matches 1 to 0, out-of-scope false positives still 0/8, 29/29 + 18/18 + 11/11.

  Then tried to fix the rest by weighting word rarity into the score (a question sharing "arthur", a word unique to exactly one FAQ entry, should match it). Swept eight weight combinations across seq_ratio/overlap/rarity: **every single one scored identically, 5/16.** The weights were irrelevant because the real blocker is the foreign-word veto added earlier the same session. "why did arthur fail" has keywords {arthur, fail}, "fail" appears nowhere in the FAQ, so one foreign word against one shared word trips the veto before scoring happens at all. The veto is costing recall on exactly the cases rarity would rescue.

  Loosening it is not available either: the veto is what kills "is my sink blocked right now" (shares "blocked", df 1, unique to one entry) returning project status. Lexically, "arthur"+"fail" and "blocked"+"sink" are the same shape, one rare shared word and one unknown word. Nothing about word frequency, length, or sentence shape separates them, only meaning does. That is the semantic wall reached from a second, independent direction, precision and recall in direct tension with no lexical knob between them. Stopped tuning rather than trading one bug class for the other.

  Standing conclusion: remaining recall needs real embeddings. Cheap lexical wins are now exhausted, 5/16 is the honest ceiling for this approach.

- **2026-09-14, semantic FAQ matching shipped: recall 5/16 to 13/16, the wall came down.** Two independent measurements this session concluded lexical matching had nothing left (see the two entries above). Built the real fix.

  Picked the tool by measuring rather than assuming. oMLX on :8000 is already warm and exposes `/v1/embeddings`, but rejects the Qwen chat models with "is not an embedding model", so it needs a dedicated one either way. Ollama is already on this machine (powering Conveyer and the cheap-fix skill), so pulled `nomic-embed-text`, 274MB, 768 dimensions. Measured latency before building anything: **0.069s warm, 10.5s cold** (model load). Warm is the same order as the 0.04s lexical path, so the fast path survives.

  Swept the cosine threshold on the same 16 paraphrases plus the 8 out-of-scope questions: 0.68 gives 13/16 with 0 false positives, 0.70 gives 12/16, and the highest out-of-scope score measured is 0.657 ("is my sink blocked right now"). **Took 0.70, not the better-scoring 0.68**, for margin: this project treats a confident wrong answer as strictly worse than a miss, and 0.023 of headroom on an 8-sample precision set is too thin to trust.

  The deadlock from the previous entry is genuinely resolved, not worked around. "why did arthur fail" scores 0.850 and "is my sink blocked right now" scores 0.657, cleanly separated, where lexically both are one rare shared word plus one unknown word and no rule could tell them apart. Cases with literally zero shared vocabulary now work: "who works on this" to "Who maintains this project?" (0.735), "what do you use to train it" to "What tool actually runs training?" (0.764).

  Semantic runs first, lexical still runs on a semantic miss. The union measured better than either alone (13/16 vs 12/16 semantic, 5/16 lexical) because they fail on different questions, and the lexical path is already precision-hardened to 0/8, so the fall-through costs nothing. FAQ header vectors are cached to a gitignored `.faq_embeddings.json` keyed by a hash of the headers, so editing FAQ.md invalidates it automatically with no rebuild step to forget.

  Degradation was tested, not assumed: with nothing listening on the embed port and no cache file, `faq_match()` still answers correctly via the lexical path in 0.01s. A fresh clone with no Ollama behaves exactly as it did yesterday. Query timeout is a deliberate 8s, shorter than the 10.5s cold load, so the worst case is one lexically-matched answer while Ollama warms in the background rather than a 10-second hang.

  **Final: 13/16 recall (from 3/16 when this session started), 0 wrong entries, 0/8 false positives, 29/29 + 18/18 + 11/11.**

  Remaining 3 misses, honestly: "does it have a full screen terminal ui" (0.492, the entry says "TUI" and the embedding model doesn't expand the acronym), "what counts as success here" (0.616), "does training cost money" (ranks the training-tool entry above the cost entry). All are genuine semantic near-misses, not bugs, and all are below threshold rather than confidently wrong, which is the correct failure direction.

- **2026-09-14, live-tested the semantic path through a real conversation and gated it against regression.** Embeddings had only ever been exercised through eval harnesses, never an actual `chat.py` session, and every previous session found real bugs precisely that way. Ran a four-turn conversation across the risky routes: the officeholder lookup still wins over a semantic FAQ match ("who is the current prime minister of canada" to Mark Carney), general knowledge still routes out ("what is the capital of france", 0.460, correctly below threshold), and "who works on this" now answers correctly, a case that was a hard miss before tonight. No routing bug found, which is worth recording as a negative result rather than assumed.

  Two suspected problems measured and dismissed rather than "fixed": `_faq_vectors()` re-reads and re-parses a 412KB cache file on every single question, which looked like an obvious latency bug, but a full `faq_match()` call measures 0.015s (cache parse 0.002s), *faster* than the documented 0.04s lexical path because a semantic hit returns before the lexical path runs at all. Cache invalidation was likewise verified rather than trusted: editing the header list rebuilds (41 to 42 vectors) and reverting rebuilds back, so a FAQ.md edit mid-session cannot serve stale vectors.

  Real gap closed: `eval/faq_paraphrase.py` measured recall but could not enforce it, so nothing stopped a future matcher change from quietly walking 13/16 back to 3/16. Added `--min N` (exits nonzero below the floor) and wired it into `hooks/pre-commit` for `ask.py`/`FAQ.md` changes. Gate verified in both directions, `--min 99` exits 1, `--min 13` exits 0. It skips itself when the embedding daemon is unreachable, since that is a degraded environment and not a regression, otherwise any machine without Ollama would be unable to commit. Deliberately not added to CI, same reason `eval/score.py` isn't: a standard runner has no Ollama and no Apple Silicon.

  Known and unfixed, honestly: "what counts as success here" (0.616) falls under threshold to generation and rambles. It's one of the three documented semantic near-misses, failing in the correct direction (a miss, not a confident wrong answer), and lowering the threshold to catch it costs the precision margin that keeps "is my sink blocked right now" (0.657) out. 29/29 + 18/18 + 11/11 + 13/16.

- **2026-09-14, fixed the Wikidata resilience gap logged at v0.7.4 and left open since.** Reproduced it first by stubbing every request to fail: "who is the current prime minister of canada" answered with FAQ.md's own entry *describing this very feature* ("Yes, as of 2026-09-13. DDG and a plain Wikipedia summary both describe the office..."), documentation about itself served as though it were the answer. Semantic matching had quietly made this more likely to fire, since that FAQ entry is near-identical in meaning to the question it was hijacking.

  Root cause was one layer below where the symptom showed: `http_json()` returned `None` both when the request failed and when the service answered with nothing, so `current_officeholder()` could not tell "Wikidata is down" from "this isn't an officeholder question", and the caller could only read either as "carry on, try the FAQ". Fixed at that layer: `http_json()` takes an optional `on_error` sentinel (default `None`, so every existing caller is untouched), `current_officeholder()` passes `FETCH_FAILED` on all three of its requests and returns `(None, UNREACHABLE)` for a genuine outage. Both callers, `ask.py` and `chat.py`, now return an honest "couldn't reach Wikidata, not going to guess at a name" instead of falling through.

  Verified in all four directions rather than just the happy path: under a simulated outage both `ask()` and `chat.py` admit it, with the network restored the real answer still comes back ("Mark Carney"), and a project who-question ("who maintains this project?") still resolves through the FAQ rather than newly claiming the network is down. Two regression tests added to `test_chat.py`, both covering opposite halves of the distinction, and both stay offline (the stub makes requests fail rather than issuing them). 13/13 + 29/29 + 18/18 + 13/16.

### Phase 7: Answer a basic question (started 2026-09-14)

Everything measured before tonight was about the project's own docs or FAQ phrasing. None of it touched what a person actually tries first: asking something simple. Built `eval/basic_questions.py`, 19 questions a child could answer (arithmetic, capitals, "who is steve jobs", how many legs a spider has). **Baseline: 9/19 right, 6 confidently wrong, 4 declined. Now 16/19 right, 3 wrong, 0 declined.**

- **Caught a real regression I had just shipped.** All four "who is <person>" questions declined with "I couldn't reach Wikidata". The outage-honesty fix from the previous entry was too broad: `current_officeholder()` fires on every "who is X", not only offices, so when running 19 questions back to back rate-limited Wikidata, every person question declined at once even though Wikipedia answers them perfectly. The fix had turned a misleading answer into no answer, which is better but still wrong. Corrected: on an unreachable lookup, skip the FAQ (the original bug, still fixed) but let the encyclopedia try, and only admit defeat if it has nothing. Added `skip_officeholder` so this doesn't fire a second identical Wikidata round trip, which matters precisely when rate-limited. Both regression tests still pass, because with every request stubbed to fail the encyclopedia has nothing either and it still declines honestly.

- **Arithmetic is now answered locally and exactly.** "what is 2+2" was being sent to the web and came back as a Danganronpa game; "what is 10 times 7" came back as The New York Times, the word "times" matching a newspaper. Questions with one exact answer should never have been a search query. `arithmetic()` normalizes word operators and evaluates by walking a parsed AST against an explicit node whitelist, not `eval()` on user input; `what is __import__("os").system(...)` returns None and falls through, verified.

- **Wikipedia title-coincidences now decline instead of answering.** Its fulltext search ranks by title match, so a question phrased as a sentence finds whatever is *named* like the sentence: "how many days are in a week" returned Bodybuilding.com. Added a plausibility guard, if a fact-shaped question ("how many X", "what color/year/temperature") shares no content word with the article, it's a coincidence and gets declined. It cannot turn a right answer wrong, only confident nonsense into an honest miss.

- **The 3 remaining failures are the semantic wall again, third sighting.** "what color is the sky" still returns a country album titled "What Color Is Your Sky", and the guard cannot catch it because the article genuinely shares "color" and "sky", it is a title coincidence that is lexically identical to a real match. "largest planet in the solar system" returns a related-but-non-answering list article. The third isn't Wikipedia at all: DDG's own Answer field returns "boomin' ice cream, sweetie!" for the boiling point of water.

- **2026-09-14, tried searching Wikipedia's top 3 instead of top 1, measured it, reverted it.** The top hit is ranked by title match, so a real article can sit behind a title coincidence; taking the first of three whose summary passes the plausibility guard looked like a clear win. It measured worse. "what is the chemical symbol for gold" went from correct to answering **"silver"**, because hits 2 and 3 are usually *related but wrong* rather than better. It also tripled request volume per question and made Wikipedia's rate limiting fire mid-run. Reverted to `srlimit=1` with the reasoning left in the code so nobody re-tries it, and confirmed the snippet idea that motivated it is still real (the Celsius snippet does contain "100 represented the boiling point", the summary doesn't), just not reachable this way.

- **The harness was rate-limiting itself, so the score wasn't a measurement.** Identical code scored 16/19 and then 11/19, the difference being that a flat-out run throttles Wikidata partway through and every "who is <person>" question declines at once. Paced the harness at 3s between questions. Confirmed the knock-on holdout failure ("who's the prime minister of canada" missing "Carney") was purely 429s and not a regression: once Wikidata recovered, the same query answered "Mark Carney (Prime Minister of Canada)" correctly. Worth remembering before trusting any future number from this file, and worth noting that a hammered run also pushed the generation path into inventing a fake second model ("Alice ... built 2026 09 17"), which is the 0.5B ceiling showing through whenever lookups fail.

- **2026-09-14, multi-turn general knowledge was broken, and it's the one thing chat.py exists for.** Live-tested a normal conversation: "who is steve jobs" answered correctly, then "what company did he found" returned **the 1997 slasher film "I Know What You Did Last Summer"**. Root cause: chat.py's conversation memory only ever covered *project* topics (the sticky `topic_active` flag). A general-knowledge follow-up had no referent resolution at all, so "he" went to Wikipedia as a literal word and the title search matched the sentence's own wording. Every prior session's memory work had been about project continuity, and nobody had asked a two-turn question about the outside world.

  Fixed with `resolve_followup()`, substituting the first pronoun with the previous general-knowledge subject ("what company did he found" becomes "what company did steve jobs found"). `last_subject` is threaded through `answer_turn()` as an optional argument defaulting to None, so one-shot callers and the tests behave exactly as before, and it's cleared when the conversation turns back to the project so a stale person can never be substituted into a project question. Three tests added (16/16).

  Honest result: the follow-up is now *topically* right (it returns the 2015 Steve Jobs biopic) instead of a completely unrelated film, but still not the actual answer "Apple", because searching "what company did steve jobs found" title-matches the movie. Real improvement, same documented search-summary ceiling underneath. Also confirmed the earlier holdout dip was purely rate limiting: back to 18/18 once Wikidata recovered. 16/16 + 29/29 + 18/18 + 13/16.

- **Audited the stemmer for bad collisions, found none.** `_stem()` over-strips visibly ("water" to "wat", "celsius" to "celsiu"), which looked like a latent false-match risk. Grouped all 798 distinct words across FAQ questions and answers by stem: 88 stems cover more than one word, and every single group is a legitimate morphological family (train/trained/training, match/matcher/matching, score/scores). No unrelated pair merged. Recording it as a checked negative result so it doesn't get "fixed" later on appearance alone.

- **2026-09-14, the worst bug of the session, and I had shipped it myself hours earlier.** Live conversation: "who is taylor swift" answered correctly, then "and her net worth?" found nothing and fell through to generation, which invented "Turing. Not a model." **and latched `topic_active` to True.** From that point every question in the session was treated as project-scoped, so a typo'd "what is teh capitol of frnace" answered **"the Turing project"**. One failed outside question permanently converted the whole conversation.

  Root cause was the latch rule added earlier tonight: `if faq_answer or (not holder_answer and not gk_answer and answer)`. That second clause is the bare generation fallback, which is where *every* unanswered question lands, including an outside question the encyclopedia simply couldn't answer. It was written to mean "this was answered from project material" and actually meant "nothing else handled this". Fixed to latch only on real evidence: an FAQ match, a project extraction, or a question already in scope. Verified both directions, the failed-follow-up case no longer latches, and the original case the latch exists for ("how do I boot into you" then "what if I don't pass any flags") still stays in project scope.

  That exposed a second, independent bug underneath: even unlatched, an out-of-scope question with no encyclopedia result still fell to generation, and generation runs on *project retrieval context*, so it can only produce a project-flavoured answer to an outside question. Now a question that isn't project-scoped and found nothing says so instead of inventing. Project questions still generate normally.

  And a third, found in the same conversation: a bare "why" returned "Why.., the first extended play by the South Korean boy band BoyNextDoor". A question with no content word left after stopwords has nothing to look up, but DDG and Wikipedia will title-match it to something regardless. `general_knowledge()` now returns nothing for a contentless query, checked after arithmetic since "2+2" has no word characters either and is genuinely answerable. Fixed at that layer so `ask.py` benefits too, not just chat.py.

  16/16 + 29/29 + 13/16. The holdout run shows 17/18 with "who's the prime minister of canada" missing "Carney", verified as Wikidata rate limiting and not a regression: a direct call returns `UNREACHABLE`, and the answer is the honest decline, which is correct behaviour under an outage.

- **2026-09-14, the sticky topic flag had no exit, so it swallowed genuine topic changes.** Live: "what is turing" answered correctly, then "what is the capital of japan" answered **"the project."** The v0.7.3 sticky flag was built so a keywordless follow-up stays in project scope, and its only carve-out was a "who is the current X" query. Any other change of subject was trapped.

  Fixed with a second carve-out using the same foreign-word test that fixed `faq_match`'s false positives: if a question's content words are *all* absent from the project's own vocabulary, it's a topic change, so drop scope. First attempt built that vocabulary from FAQ questions *and* answers, and it didn't work, because the answers discuss their own examples (a past write-up mentions "the capital of France"), so "capital" counted as project vocabulary. Headers are what the FAQ is about; answer prose can mention anything. Questions only.

  Verified all four directions: the switch now breaks out, the conversation can come back to the project afterwards ("what is samantha" re-enters scope), and both cases the sticky flag exists for still hold ("How confident does a match need to be?" shares "match", "what if I don't pass any flags" shares "flags"). Two tests added. 18/18 + 29/29 + 18/18 + 13/16, with Wikidata recovered so the holdout is back to a clean 18/18.

- **2026-09-14, a follow-up after an honest decline answered with a Bill Murray film.** Tested the conversation shape nobody had: ask something unanswerable, then follow up on it. "asdkjfhaskdjfh" correctly declined, then "what about that" returned **"What About Bob?, a 1991 American comedy film"**. Root cause: "about" is pure filler but five letters long, so it survived the length filter and read as content. Every other word in that question is already a stopword, so "about" alone carried the search straight into a title match. Added it to `_STOPWORDS`, which leaves the question with no content word at all and hands it to the contentless-query guard added earlier tonight. Checked first that no FAQ header depends on the word. Both turns now decline honestly.

  Also confirmed working in the same pass, worth recording as negative results: a fragment follow-up after a project answer ("what is samantha" then "and the eval score?") resolves correctly, an unrelated topic switch mid-conversation works, the same question asked twice in a row is stable, and a non-English question ("cual es la capital de espana") declines rather than inventing.

- **2026-09-14, FAQ.md was quoting stale eval numbers at users.** "What's the current eval score?" still answered "29 out of 29 ... 13 out of 13" from the 2026-09-13 retrain, while the held-out set had grown to 18 and two entirely new harnesses existed. A project whose whole point is not stating things confidently without checking should not ship a stale self-report. Rewritten to name all four harnesses and their real numbers, including the unflattering ones (13/16 paraphrase, 16/19 basic questions) and *why* the two perfect scores look perfect: their prompts are worded the way the FAQ words things, which is exactly the bias the other two harnesses were built to expose. 18/18 + 29/29 + 18/18 + 13/16.

- **2026-09-14, Samantha is callable from other apps now, and the integration found a bug in her first minute.** Joshua asked whether she could be piped into Nimble, which has pluggable AI engines. Read Nimble's `AIEngine.swift` first rather than guessing: its local-engine branch POSTs to `base + /v1/chat/completions` with the OpenAI message shape and reads `choices[0].message.content`. So the right move was to implement the wire format Nimble already speaks rather than add a Samantha engine to Nimble, which means **zero changes to Nimble**, point its Ollama engine at the port and it works.

  `serve.py`, stdlib `http.server`, no framework for a single-user localhost endpoint. Serves `/v1/chat/completions` and `/v1/models`, sends CORS headers so Nimble's web build can call it too, and returns sources in a `turing_sources` field that OpenAI-shaped clients simply ignore. Declines map to the literal string `"UNKNOWN"`, which is Nimble's own signal for "this engine had nothing", so it falls through to another engine instead of showing a refusal sentence to the user. The caller's system prompt is deliberately ignored: Samantha has her own persona and a fixed pipeline, and honouring an arbitrary one would misrepresent what is being served.

  **The integration immediately earned itself.** "asdkjfhaskdjfh" came back through the API as garbled half-Chinese text, while `chat.py` declined the same input cleanly. Cause: the out-of-scope generation guard added earlier tonight went into `chat.py` only, and `ask.py` still fell through to generating from project retrieval context. Two entry points, two behaviours, in a codebase whose own comments say they must never drift. Fixed in `ask.py` where both paths share it, and moved `OUT_OF_SCOPE` into `ask.py` so `chat.py` imports it rather than owning it. Exactly the kind of divergence only a third caller surfaces, which is the argument for having one. 18/18 + 29/29 + 18/18 + 13/16.

- **2026-09-14, "does the pipe still work" is now one command.** Joshua's actual ask was to re-test the Nimble integration every few versions, which only happens if it's cheap. `test_nimble.py` starts `serve.py` on its own port, fires the **exact** request Nimble builds in `AIEngine.swift` (same URL, same body, and Nimble's real system prompt copied verbatim rather than paraphrased, so a drift on either side shows up), and parses the reply the way Nimble's own `parse()` does. Five checks: the model is advertised, a project question returns usable text, a general-knowledge question works end to end over HTTP, arithmetic still resolves locally, and a decline surfaces as `UNKNOWN` so Nimble falls through to another engine instead of rendering a refusal as an answer. Wired into `hooks/pre-commit` for `serve.py` *and* `ask.py` changes, since `serve.py` is a thin wrapper over `ask()` and an `ask.py` edit can break the integration without touching `serve.py`. Documented in FAQ.md so it's discoverable rather than folklore.

- **2026-09-14, Nimble's web build could never have reached Samantha, and reading the code found it faster than a browser would have.** Before driving Chrome to prove the UI link, read `docs/engine.js` against `AIEngine.swift`. The two shapes are identical (same URL, same body, same `choices[0].message.content` parse, same `UNKNOWN` handling), so nothing to reconcile there. But the web build calls from a browser, and a POST carrying `content-type: application/json` triggers a CORS preflight. `serve.py` sent `allow-origin` and `allow-headers` but **not `allow-methods`**, which Chrome rejects outright. The native build worked perfectly over HTTP the whole time while the web build could not have connected at all, which is exactly the kind of gap plain `curl` never shows you.

  Fixed, plus `access-control-max-age` so browsers stop re-preflighting every call. Added a preflight check to `test_nimble.py` and then **verified the check can actually fail**: removed `allow-methods`, watched it report "a browser caller would be blocked" and exit 1, restored it, watched it pass. A test that has never failed isn't evidence of anything. 18/18 + 29/29 + 13/16 + Nimble 6/6.

- **2026-09-14, new failure class: Wikipedia disambiguation pages.** Live-tested a batch of ordinary questions. "what do bees make" returned **"Bees Make Honey may refer to:Bees Make Honey (band), British band"**, a disambiguation page served as an answer. These are lists of things sharing a name and are never an answer to anything, and Wikipedia labels them itself (`type: "disambiguation"` on the summary endpoint), so rejecting them needs no heuristic at all. Fixed, and added the question to `eval/basic_questions.py` as a regression case. It now declines honestly rather than naming a band, which is the correct direction even though it is still not the right answer.

  Same batch, same known ceiling, logged not chased: "largest country in the world" returns a ranked-list article, "when was the internet invented" returns **Al Gore**, and "how tall is mount everest" appeared to do the same. **Correction, 2026-09-14: the Everest case was my error.** Its summary does give the figure, "8,848.86 m", further into the same paragraph than the opening I had read. That question was answering correctly the whole time. The other two are the search-summary architecture answering "here is the topic" to a question asking "here is the value", which is the documented Phase 7 ceiling.

  18/18 + 29/29 + 13/16 + Nimble 6/6.

- **2026-09-14, three more real bugs from four ordinary questions.** Tested statements and non-question shapes for the first time.

  **"what year is it" returned Wikipedia's Flat Earth article.** A date question has no searchable subject, so the fulltext match landed on "Earth". The machine has a clock; no search engine should ever have been asked. Added `clock()`, same category as `arithmetic()`, answering year, month, day, time and date locally and exactly, before anything touches the network.

  **"tell me about photosynthesis" was declined as out of scope.** `is_question()` rejected it because it has no question mark and starts with a verb. That test exists for a real reason (it stops "write a commit message for X" reaching Wikipedia and matching the Git article), so rather than loosening it, added a short explicit list of lookup imperatives: tell me about, explain, describe, define. Verified the original case still declines: "write a commit message for adding dark mode" stays out.

  **Then the clock fix exposed a third bug in the pronoun resolution I added earlier tonight.** "what year is it" then "what day is it" answered **2026 twice**. Two causes compounding: `subject_of("what year is it")` couldn't strip anything and returned the whole question as the "subject", then `resolve_followup` substituted it into the next question, producing "what day is what year is it", which re-matched the year pattern. The "it" in these questions is a dummy subject, not a reference to a previous turn. Added `self_contained()`, a question answerable locally by clock or arithmetic never refers back and never supplies a subject to refer back to, so it is skipped on both sides of the resolution. Verified clock questions and genuine pronoun follow-ups ("who is steve jobs" then "what company did he found") both work.

  18/18 + 29/29 + 13/16 + Nimble 6/6. Holdout shows 17/18 on the Carney prompt, verified as Wikidata rate limiting again (a direct call returns UNREACHABLE), not a regression.

- **2026-09-14, unit conversions, and a gating bug they exposed.** "how many kilometers is 5 miles" returned an article about **available seat miles**, an airline capacity metric. Conversions are the same category as arithmetic and clock: one exact answer this machine can compute, that a search engine can only get wrong. Added `convert()` with a deliberately small table of units people actually ask about rather than a units library, and it refuses cross-dimension nonsense ("how many kg is 5 miles" returns nothing rather than a confident number).

  Then the fix didn't work in `chat.py` while working in isolation, which exposed the real bug. `convert()` lived inside `general_knowledge()`, which is gated behind `is_question()`, and "convert 100 fahrenheit to celsius" is not question-shaped, so it was declined as out of scope while the identical conversion answered fine when phrased as a question. That gate exists to stop a task instruction reaching Wikipedia and matching a loosely-related article, which is a real risk for a *search* and no risk at all for a calculator. Pulled all three exact paths into `local_answer()` and call it first in both `ask()` and `answer_turn()`, before any gate. Verified the gate still does its job: "write a commit message for adding dark mode" still declines.

  Logged, not fixed: a follow-up conversion ("convert 100 fahrenheit to celsius" then "what about in kelvin") returns a Kenyan marathon runner named Kelvin. Carrying a value across turns is real conversational state that doesn't exist yet.

  18/18 + 29/29 + 18/18 + 13/16 + Nimble 6/6.

- **2026-09-14, CI had been red on every push and the cause was a hardcoded home path.** `test_scope_stays_active_across_keywordless_followup` failed on GitHub while passing locally. `FAQ_PATH` (and `ADAPTER`, and both `mlx_lm.generate` calls) pointed at an absolute `~/Documents/Code/turing/...`, so on a runner the FAQ loaded as an empty list, `project_vocabulary()` came back with no FAQ words in it, and the topic-change carve-out added earlier tonight concluded every question was a change of subject. The test was right; the paths were wrong.

  Not just a CI problem: any clone anywhere but that exact directory was silently degraded, which is a real portability bug the test happened to catch. All project-local paths now derive from `REPO = os.path.dirname(os.path.abspath(__file__))`.

  Verified properly rather than by assumption. A plain `git clone` on this machine passes *for the wrong reason*, since `~/Documents/Code/turing/FAQ.md` still exists and the absolute path resolves. Re-ran with a fake `HOME` instead: the old hardcoded path reproduces CI's exact `AssertionError`, the new path passes 18/18.

- **2026-09-14, Joshua pointed out Nimble might be closer to this than it looks. He was right, and it's a ladder failure on my part.** Nimble's `docs/engine.js` `answer()` is the *same architecture* this project has been rebuilding from scratch all night: exact local answers first, then pattern-gated live sources, then a model, then an honest nothing. Nimble runs convert, math, graph, currency, dictionary/weather/localTime, then LLM+DDG, then wiki, then `{kind:"none"}`. Samantha runs arithmetic, clock, convert, officeholder, FAQ, retrieval, generation, then a decline. Same shape, arrived at independently, twice.

  Concretely: I wrote `convert()` and `clock()` from scratch tonight while Nimble already had `tryConvert()` and `localTime()`, working, in both JS and Swift. That is the "check whether it already exists in the codebase" step skipped, and it cost real work.

  **The more valuable find is one line of Nimble's, engine.js:206:** *"A model's number is an unsourced guess: for numeric answers prefer DDG when it has one."* It resolves the exact ceiling documented repeatedly above, where a search-summary answers "here is the topic" to a question asking "here is the value" (Everest's height, largest country, boiling point of water). Nimble's answer is not a smarter search, it's preferring whichever source actually contains a number when the question wants one. That is directly portable here and does not require the 8B decision.

- **2026-09-14, a clean measurement run caught two regressions I had just shipped.** Ran `eval/basic_questions.py` properly after several fixes landed and got **9/20**, down from 16/19. Two were genuine, both mine from the previous hour.

  **`clock()` answered 2026 to "what year did world war 2 end".** The year pattern was a bare `what year`, so it matched any question containing those words, present tense or not. Confidently wrong about history, from a feature added to *stop* confidently wrong answers. Now requires an explicit "is it"/"is this"/"are we in".

  **The number guard rejected correct answers.** `_WANTS_NUMBER` declined any value-seeking question whose source text had no digit, which killed "how many sides does a triangle have" because the Triangle summary says "three sides" in words. Removed it entirely rather than patched: it was already recorded above as having been justified by a false premise (the Everest example that turned out to be answering correctly), and a guard with a wrong rationale that also breaks working answers has nothing left to stand on.

  **The rest of the 9/20 was not code at all.** Wikipedia had started rate-limiting this session too, on top of Wikidata. That exposed a real honesty bug worth keeping: with the network unreachable, ordinary questions came back "it's outside what I know about this project", a confident scope claim when the true cause was a dead network. `general_knowledge()` now distinguishes "searched and found nothing" from "could not reach anything to search", and both entry points say so. Same principle as the earlier Wikidata outage fix, applied one layer out.

  Verified after: with Wikipedia recovered, "how many sides does a triangle have" and "what is the chemical symbol for gold" both answer again, confirming those declines were throttling rather than the code. 18/18 + 29/29 + 18/18 + 13/16 + Nimble 6/6. **The 9/20 number itself should not be quoted**, it was measured through a throttled network; the next clean run needs a genuinely rested one.

- **2026-09-14, cached every slow path, which fixes the rate-limiting problem at its root.** Pacing the eval harness treated the symptom. The real issue was refetching identical URLs: a Wikipedia summary does not change hour to hour, and re-asking for it is what got both Wikidata and Wikipedia throttling this session badly enough that a run scored 9/20 through a dead network while the code was fine.

  `http_json()` now caches successful responses to disk for a day, keyed by URL. Failures are deliberately never cached, so a throttled minute can't freeze in for 24 hours. Measured: a repeated question went 0.93s to 0.04s, and a rerun of the eval suite makes no network requests at all for questions it has already asked. Pacing dropped from 3s to 1s as a result, only relevant on a cold first run.

  Extended to the other two slow paths on the same principle: brain's RAG search (a 15s-timeout network call) and `mlx_lm.generate` (a ~2s subprocess that reloads the model every time and returns the same text for the same prompt).

  **First clean measurement since: 13/20 right, 0 declined.** Trustworthy now in a way none of the recent numbers were.

  Also chased what looked like a fresh bug, "how many sides does a triangle have" declining while answering fine in isolation, and traced it to the throttled window rather than the code. The cache is what stops that class of phantom bug recurring.

- **Open decision, deliberately not taken unilaterally: should general knowledge route to a local 8B?** `llama3.1:8b` and `qwen3:8b` are already on this machine and would answer all 19 correctly. It is architecturally consistent (the chain already delegates facts to non-Samantha sources, and it stays on-device, no frontier API), but it would make Samantha a thin router over a model 16x her size, which cuts against what this project is. Flagging it as Joshua's call rather than quietly changing what Samantha means.

### Phase 8: Train the voice, not the facts (started 2026-09-14)

Eight runs established what this project keeps relearning: a 0.5B LoRA absorbs style reliably and facts not at all. Run 6 tripled the document data and the score stayed flat at ~1/28. Run 8 added twelve hand-picked instruction/response pairs from this repo's git log and fixed a regression more document text could not. Every real gain tonight came from retrieval, matching and routing, never from weights. So the conclusion is not "train more", it is **train the only thing weights are good at**.

- **`harvest_voice.py`, 183 real instruction/response pairs from 54 repos.** The task is learnable with no model in the loop: given the files a commit touched, write its subject line. Every response is a line Joshua actually wrote, so the voice is real by construction rather than synthesized. That is 15x the hand-picked set, and it regenerates as the fleet grows.

  Deliberately excluded: merge commits, dependency bumps, version-only subjects, and **anything carrying an AI co-author trailer, including this session's own commits**. Training a model to imitate its own prior output is how a voice collapses into an average of itself.

  One real bug while building it: the first run produced instructions naming things that were never files ("a change to session_01NzZB5DGtSRrhc2WYNQ3qAm"), because a commit body containing a URL or session id parsed as part of the `--name-only` file list. Fixed by closing the body with an explicit separator in the log format so the file list is unambiguously delimited.

  Wired into `prep_data.py` as an optional source, so a fresh clone with no `data/` still trains.

- **Retrain attempted 2026-09-14 and correctly refused to start: only ~0.4GB free.** `prep_data.py` rebuilt cleanly with the harvested pairs included (295 own chunks x3 plus 100 fleet chunks, 985 total, up from 55 own chunks). But this machine has crashed mid-backprop from a clean 6.4GB free, so starting a run at 0.4GB would have taken the Mac down rather than produced a model. Deferred rather than attempted. **Next session: check free memory first, then `train_resilient.sh` (it has its own MIN_FREE_MB pre-flight and will wait out a low-memory moment with backoff).**

- **Trap to remember before measuring any new adapter:** `ask.py` now caches generation output keyed by prompt hash, so a fresh adapter measured against a warm cache returns the *old* adapter's answers. Delete `.http_cache.json` before scoring a retrain.

- Not yet done: the retrain itself, and a held-out check that the harvested pairs improve commit-message quality without regressing the FAQ/retrieval paths. More data has made things *worse* before (run 7), so this needs measuring, not assuming.

### Phase 9: Hands (started 2026-09-20)

Samantha could answer. She could not do anything. "Open chrome and go to hacker news" got "that's outside what I know about this project." Now it opens Hacker News.

- **`tools.py`, six tools: `open_app`, `open_url`, `web_search`, `current_tab`, `read_page`, `screenshot`.** No shell tool, on purpose. Every tool is a fixed argv. A string a model wrote never reaches `sh`.
- **Two layers.** `act()` is a regex router, instant and exact, same category as arithmetic and the clock: a recognisable command has one right action, so no model is involved. `agent()` handles multi-step asks ("poke around hacker news and tell me the top stories") by letting `qwen3:8b` drive the same tools through Ollama's native tool calling. The 0.5B cannot pick tools reliably. She borrows a bigger head for her hands.
- **Wired in at `local_answer()`**, the one place `ask.py`, `chat.py`, the TUI and `serve.py` all route through.
- One real bug caught before commit: the action detector matched "summarize", which hijacked the eval prompt "Summarize what Turing is in one sentence" into the agent. Narrowed the verb list, added it to `tools.py`'s self-check.
- Not yet done: `read_page` is a tag-strip over a plain fetch, so JS-rendered pages come back empty. Clicking and typing in Chrome needs a real bridge. And the goal that matters: a small model that calls these tools itself, so the borrowed 8B head can go.

### Road to 1.0.0 (set 2026-09-20)

1.0.0 means you can sit down, talk to her, and she answers and acts without a rewrite. Each box ships as its own tagged release. No box gets ticked without a check that fails when it breaks.

Shipped and off the list: a 1.7B head that answers in 5 seconds; music and personal tools; know things (62/65, 0 confidently wrong, `eval/basic_questions.py`); browser tabs (list, switch, close, read the rendered page); a real harness (`harness.py`, wired into `chat.py`, asks before writes); her own head for the first 51 tools (`hands-adapter`, `eval/hands.py`); the ten image tools smoke tested for real (`eval/smoke_images.py`).

Gaps, in order:

- [ ] **Her own head, the rest.** Attempted 2026-09-21 (docs/BAKEOFF.md "round five"): training data now exists for all 77 tools, but the retrain refused 19 right picks it used to allow, so the shipped picker still covers the first 51. Not blocking 1.0.0: the other 26 tools work through the exact router.

### Joshua's Mac to-do (only these need the keyboard)
The loop runs in the cloud and cannot reach Cloudflare or this Mac. Tick a box when done, the loop picks it up.

- [x] Deploy the site (done 2026-09-22, by hand; CI still skips until CLOUDFLARE_API_TOKEN is a repo secret): `npx wrangler deploy` in the repo (or add the `CLOUDFLARE_API_TOKEN` repo secret once and it deploys itself on every push)
- [x] ask_llm (was ask_claude): ask any LLM on this Mac by name, or the biggest (oMLX Qwen3.5-9B, then Ollama qwen3:8b), no key needed (2026-09-22)
- [ ] Try voice and GUI control on the Mac: `python3 chat.py --voice` needs the microphone once (macOS asks). GUI control once the loop builds it

### How we compete with trillion-dollar labs (set 2026-09-22)
We will not out-think Claude or GPT: that is data centres and years. We win where they structurally can't follow:
1. **It's yours and it's local.** Runs on your Mac, no account, no per-query cost, nothing leaves the machine. They can't ship that without giving away the model.
2. **Hands on your real computer.** Exact, instant tools (77 and counting) that open, edit, search, remember, and ask before writing. Frontier chat apps still mostly live in a browser tab.
3. **Never confidently wrong.** Grounded answers with sources, declines instead of guessing. A small model that says "I don't know" beats a big one that invents.
4. **Borrow their brains, legally.** Use frontier models as teachers (Phase 6 distillation) and as tools she can call when asked, so her floor rises with theirs.
5. **Tiny and fast.** 0.5B picks tools in milliseconds on a Mac Mini. Speed and reliability are the product.
Every loop iteration should move one of these five.

### Gaps found by the loop
The loop compares her with other assistants (Siri and Shortcuts, Apple Intelligence, Claude and ChatGPT desktop with MCP, Open Interpreter, Raycast AI, local Ollama agents), adds each real gap here with where it was seen, builds it, then deletes the line once it ships (history lives in git). Newest and biggest first.

- [ ] Voice, the rest: you can cut in while she speaks, and a wake word. Voice in shipped v1.8.0 (python3 chat.py --voice, Whisper on MLX). Seen in ChatGPT and Gemini voice modes
- [ ] See pictures: "what is in this photo", "read this screenshot", "what's wrong with this chart" through a local vision model (Qwen2.5-VL or Gemma 3 on oMLX/Ollama). Screen reading today is text only. Seen in every frontier chat
- [ ] Deep research: "research X" reads 5 to 10 pages, the library and her notes, then writes a short cited brief with the bigger local model. Seen in ChatGPT and Gemini Deep Research, Perplexity
- [ ] Run code for answers: stats on a CSV, a chart, a unit-heavy calculation, in a sandboxed Python that asks first. Seen in ChatGPT data analysis, Claude analysis tool
- [ ] Summarize anything long with the 9B (a PDF, a page, a thread) instead of the 1.7B reader's one sentence. Seen in every frontier chat
- [ ] Translate text and pages, offline. Seen in every frontier chat, Apple Intelligence
- [ ] Write real files: draft an email, a doc or a slide outline into Pages, Mail or a markdown file, asking first. Seen in Claude artifacts, ChatGPT canvas, Apple Writing Tools
- [ ] Read mail: "anything from the bank today", "summarize my unread" through Mail.app, read only. Seen in Gemini in Gmail, Apple Intelligence
- [ ] Remind me later without a daemon: "tell me the weather every morning" becomes a Reminder or a Shortcuts automation she sets up, asking first. Seen in ChatGPT tasks, Gemini scheduled actions
- [ ] Control the GUI with approval: click and type in an app, every step confirmed by the harness first. Seen in Claude computer use, Open Interpreter

### Phase 6: Distillation, not scale (month 4+, optional/ambitious)
Instead of chasing bigger bases, use a frontier model (Claude) to generate high-quality synthetic training examples in our exact style, then distill that into Samantha. This is literally how most useful small models are built today, nobody pretrains from raw internet text anymore if they can help it.

### What we will never do on this budget
Pretrain a foundation model from raw text at frontier scale. That needs a data-center, a research team, and normally $10M+ in compute even for a "small" frontier-adjacent model. Not the plan, the plan is a small model that's genuinely ours and genuinely useful, which is a completely different (and completely reachable) goal.

### Win condition
Not "beat GPT." Win condition is: ask it to draft something in our voice, or answer a question about one of our own projects, and the answer is actually good enough to use without rewriting it.

## Progress log

- **2026-09-13, run 1:** 588 lines (wiki + READMEs), 200 iters, raw-text format. Loss bounced 2.2-3.4, no clean convergence, too little data. Proved the pipeline works end to end, no gibberish (unlike Arthur).
- **2026-09-13, run 2:** widened sources to roadmap.md + CLAUDE.md across the fleet, 2054 lines, 500 iters, still raw-text format. Val loss down from 3.66 → 2.99 by iter 200. Eval score: 1/8.
- **2026-09-13, run 3:** switched to chat-formatted data, 500 iters. Eval score: ~1/28, and the failure mode got worse in a telling way, it started answering in *other fleet projects'* README voice, because the training data globbed the whole ~50-repo fleet with no weighting toward Turing's own docs.
- **2026-09-13, run 4:** oversampled own docs 10x (only 17 unique chunks) on top of run 3's weights, 500 more iters. Overfit hard, train loss 0.5-0.6 vs val loss 1.26, output degraded into verbatim doc parroting and garbled tokens regardless of prompt. Real lesson: neither more repeats of a small pool nor more iterations substitute for more distinct real content.
- **2026-09-13, run 5:** fresh start from base (not resumed on the overfit run 4 weights), `OWN_REPEATS` 10→3, 200 iters instead of 500. Fixed the collapse, fluent English again, no garbled tokens or verbatim parroting. But still only ~1/28 factually correct, confidently invents plausible wrong facts.
- **2026-09-13, run 6:** added `FAQ.md`, roughly tripling unique own-project chunks (19→28), fresh 200-iter run. Score stayed flat at ~1/28. Output now recognizably draws real fragments from training docs (correct filenames, correct phrases) but stitches them with confabulated details. **Conclusion after six runs**: this is a structural limit of a 0.5B LoRA on ~150-300 examples, not a tuning problem. It reliably absorbs style (the commit-message prompt lands clean every run) but not facts. Confirms the roadmap's own Phase 4 rationale empirically: retrieval, not weight memorization, is the right architecture for factual recall at this scale.
- **2026-09-13, Phase 4: working.** Built `ask.py` (retrieval-augmented generation via `brain`'s `/api/search`) and expanded `brain/ingest.mjs` to pick up `WHITEPAPER.md`/`FAQ.md`/`TROUBLESHOOTING.md`, not just README/roadmap/CLAUDE.md. Initial testing looked like a real infra bug, exact literal-text searches returned zero hits for many minutes after ingest. Also found and fixed a real, separate bug along the way: `brain`'s deployed Worker was running code from 2026-08-31 while `src/tools.ts` had been edited 2026-09-02, redeployed it. But the retrieval gap turned out to be neither of those: Vectorize's eventual-consistency window is real and longer than it looked, a controlled test (unique marker phrase, patient 90s wait) confirmed the index does update, just not instantly. Directly re-posted Turing's 23 chunks and waited properly, they now rank top of search results (0.71-0.74) for their own content.

  `ask.py` end to end: asked "What is Turing?", got a correctly grounded, sourced answer quoting the actual FAQ.md content instead of inventing an app. Not perfect, "What went wrong with Arthur?" pulled in irrelevant wiki noise alongside the right source and got confused, retrieval query tuning and better source filtering is real follow-up work. But this is a genuine, qualitative leap from confidently-wrong memorized guesses to grounded, sourced, mostly-correct answers. Phase 4's core premise (retrieval beats memorization for facts at this scale) is now empirically demonstrated, not just argued.

- **2026-09-13, structural fix: FAQ-matcher.** Query-biasing and hand-written `FIXED_FACTS` got the score to ~10/28, whack-a-mole, one line of code per failing question. Built `faq_match()` instead: parses `FAQ.md`'s existing `## Question` / answer pairs, fuzzy-matches the incoming question (`difflib`, threshold 0.55), returns the real answer verbatim when confident, skips retrieval and generation entirely for a match. Score jumped to ~22-23/28. See `eval/results-2026-09-13-faq-match.md`. Real lesson: for a small model, a well-maintained FAQ beats fancier retrieval or training tricks for the questions people actually ask most. Task-style prompts (write a commit message, write a journal entry) correctly don't FAQ-match and still hit the generation-quality ceiling, that's expected, not a bug.

- **2026-09-13, general-knowledge fallback.** Ported nimble's DDG-instant-answer-then-Wikipedia pattern to `general_knowledge()` in `ask.py`, so Samantha can answer things outside our own docs, not just project facts. Found and fixed a real ordering bug first: tried it before project retrieval, so "Turing" (also Alan Turing's real name) got hijacked by the Church-Turing thesis article. Fixed with an explicit `PROJECT_KEYWORDS` gate, a retrieval-score threshold didn't work since the query-biasing prefix inflates scores roughly equally regardless of real relevance. This is also what caught and fixed `faq_match()`'s precision bug (see the QA infra entry above): the new `eval/score.py` scorer, run while testing this feature, immediately flagged the wrong-entry false-positive that manual eyeballing had missed. Genuine, confirmed limitation, not chased further tonight: Wikipedia fulltext search picks the wrong disambiguation for ambiguous subjects sometimes ("who wrote Romeo and Juliet" returned Tchaikovsky's overture, not Shakespeare), and neither DDG nor a plain Wikipedia summary reliably knows who currently holds a given office. See `eval/results-2026-09-13-general-knowledge.md`.
- [ ] Painting hands (pixelmator/): paint the Last Supper at 4000 layers, polish the Mona Lisa, rebuild the bcgd logo as a text-layer spec, speed probes (group layers, fill inside make)
- [ ] Picker trick: constrained output. Let her only emit a real tool name and an argument copied from the sentence (mlx_lm logits processor), so a wrong-shaped pick is impossible instead of caught after
- [ ] Picker trick: train on her mistakes. Write NEW templates for the kinds of wording she misses. Never copy items from hands-data/test.jsonl into training, that would fake the score
- [ ] Picker trick: teacher and student. Have the local 8B write a few hundred varied phrasings per tool with labels, train on those alongside the templates
- [ ] Picker trick: more "not a command" and "not sure" examples so she abstains instead of guessing
- [ ] PaintBar: share its background setting with Samantha's own paint tool, add launch at login

### 100 tools (set 2026-09-21)

The model picks, the code does. More menu is more power. Rules: every tool ships with cases in `eval/actions.py` and phrasings in `gen_hands_data.py`, the gate baseline only moves up, anything that writes, sends or deletes asks first through the harness, stdlib and built-in macOS commands only.

- [ ] Two-step picking: she picks a family first, then a tool inside it, so no single choice is bigger than about twelve. Retrain the picker for it and score it before adding tools in bulk
- [ ] Image family through Pixelmator (`edit_image` with a fixed menu): remove background, upscale, auto enhance, crop, resize, rotate, flip, grayscale, convert format, export
- [ ] Files family: find a file, move, copy, rename, zip, unzip, folder size, disk space, recent downloads, move to Trash (asks first)
- [ ] System family: dark mode, wifi, bluetooth, do not disturb, lock, sleep, brightness, uptime, ip address, running apps, quit an app
- [ ] Organizer family: list reminders, complete a reminder, add a calendar event, tomorrow's calendar, search notes, append to a note
- [ ] Browser family: list tabs, switch tab, close tab, read the rendered page, download a file
- [ ] Dev family: git status, recent commits, run a repo's tests, open PRs, open a repo in the editor
- [ ] Knowledge family: define a word, translate, convert units, time in a city, calculate
- [ ] Shortcuts bridge: `run_shortcut(name)` runs any Apple Shortcut, so one tool opens hundreds of actions the user already owns
- [ ] MCP both ways: serve her tools over MCP so other assistants can use her hands, and let her call MCP servers so any MCP tool becomes hers
- [ ] Blender family (Blender 5.2 LTS is installed, runs with no window: `blender -b -P script.py`): render a scene to an image, make a simple 3D object from a fixed menu, turn a logo into 3D text, convert between 3D formats, report what is in a file. Same rules as every family: new file, mocked unit tests that pass on Linux, one live smoke run each
- [ ] Sharp paintings: 20,000 layers removes the blocky look and 60,000 looks photographic (simulated offline). Grouping layers was measured SLOWER (460 s against 336 s flat for 2000 layers) and reverted. Hiding the app is the one proven speed-up. Next idea to measure: merge same-color neighbor cells so each layer buys more picture

